{
  "title": "OCR for pins at google maps",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "base64Image": {
      "title": "Base64 image data",
      "type": "string",
      "description": "data:image/png;base64 content, google to see online encoders available",
      "editor": "textarea"
    },
    "saveImage": {
      "title": "Save image",
      "type": "boolean",
      "description": "Save images from input to KV store as image file",
      "default": true
    }
  },
  "required": ["base64Image"]
}
